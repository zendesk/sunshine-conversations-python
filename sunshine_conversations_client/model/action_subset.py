# coding: utf-8

"""
    Sunshine Conversations API

    The version of the OpenAPI document: 17.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from sunshine_conversations_client.model.buy import Buy
from sunshine_conversations_client.model.link import Link
from sunshine_conversations_client.model.postback import Postback
from sunshine_conversations_client.model.webview import Webview
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

ACTIONSUBSET_ONE_OF_SCHEMAS = ["Buy", "Link", "Postback", "Webview"]

class ActionSubset(BaseModel):
    """
    ActionSubset
    """
    # data type: Buy
    oneof_schema_1_validator: Optional[Buy] = None
    # data type: Link
    oneof_schema_2_validator: Optional[Link] = None
    # data type: Postback
    oneof_schema_3_validator: Optional[Postback] = None
    # data type: Webview
    oneof_schema_4_validator: Optional[Webview] = None
    actual_instance: Optional[Union[Buy, Link, Postback, Webview]] = None
    one_of_schemas: Set[str] = { "Buy", "Link", "Postback", "Webview" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = ActionSubset.model_construct()
        error_messages = []
        match = 0
        # validate data type: Buy
        if not isinstance(v, Buy):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Buy`")
        else:
            match += 1
        # validate data type: Link
        if not isinstance(v, Link):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Link`")
        else:
            match += 1
        # validate data type: Postback
        if not isinstance(v, Postback):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Postback`")
        else:
            match += 1
        # validate data type: Webview
        if not isinstance(v, Webview):
            error_messages.append(f"Error! Input type `{type(v)}` is not `Webview`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in ActionSubset with oneOf schemas: Buy, Link, Postback, Webview. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in ActionSubset with oneOf schemas: Buy, Link, Postback, Webview. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # use oneOf discriminator to lookup the data type
        _data_type = json.loads(json_str).get("type")
        if not _data_type:
            raise ValueError("Failed to lookup data type from the field `type` in the input.")

        # check if data type is `Buy`
        if _data_type == "buy":
            instance.actual_instance = Buy.from_json(json_str)
            return instance

        # check if data type is `Link`
        if _data_type == "link":
            instance.actual_instance = Link.from_json(json_str)
            return instance

        # check if data type is `Postback`
        if _data_type == "postback":
            instance.actual_instance = Postback.from_json(json_str)
            return instance

        # check if data type is `Webview`
        if _data_type == "webview":
            instance.actual_instance = Webview.from_json(json_str)
            return instance

        # deserialize data into Buy
        try:
            instance.actual_instance = Buy.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Link
        try:
            instance.actual_instance = Link.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Postback
        try:
            instance.actual_instance = Postback.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Webview
        try:
            instance.actual_instance = Webview.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into ActionSubset with oneOf schemas: Buy, Link, Postback, Webview. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into ActionSubset with oneOf schemas: Buy, Link, Postback, Webview. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], Buy, Link, Postback, Webview]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


